declare global {
    interface Window {
      // google: {
      //   translate: {
      //     TranslateElement: new (options: { pageLanguage: string }) => void;
      //   };
      // };
      // googleTranslateElementInit?: () => void; // Add this line
    }
  }